<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
         failOnWarning="true"
         failOnRisky="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <!--
            memory_limit lives HERE and nowhere else, on purpose.

            There is no php.ini in this repo and no PHP container to bake one
            into: docker-compose.testing.yml provides MySQL only, and CI takes
            PHP from setup-php with no ini-values. The php:8.4 image this suite
            runs under defaults to 128M with no configuration file loaded at
            all, which is what the parallel suite was dying on.

            Passing the ini on the `php` command line does NOT work, and not
            only under parallel: `artisan test` always hands the run to a child
            process, so the flag stays with the artisan parent and the tests
            never see it. Measured both ways on this image, serial and
            parallel, and the worker reported 128M in both.

            This block does reach them, because the child rebuilds its own
            configuration from this XML and applies the block itself
            (ApplicationForWrapperWorker::execute, then
            PhpHandler::handleIniSettings). That makes the XML the only place
            a raised limit actually lands.

            TestEnvironmentTest asserts this from inside the process that runs
            the tests, so a regression fails the suite instead of resurfacing
            as an OOM weeks later.
        -->
        <ini name="memory_limit" value="512M"/>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="BROADCAST_CONNECTION" value="null"/>
        <env name="CACHE_STORE" value="array"/>
        <env name="DB_CONNECTION" value="mysql"/>
        <env name="DB_HOST" value="127.0.0.1"/>
        <env name="DB_PORT" value="3306"/>
        <env name="DB_DATABASE" value="lendyph_testing"/>
        <env name="DB_USERNAME" value="root"/>
        <env name="DB_PASSWORD" value=""/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
        <env name="NIGHTWATCH_ENABLED" value="false"/>
    </php>
</phpunit>
